global:
  scrape_interval: 10s
  scrape_timeout: 5s

scrape_configs:
  - job_name: spring-boot
    metrics_path: /actuator/prometheus
    scheme: http
    static_configs:
      - targets:
          - host.docker.internal:8080
